llm: ${sub|text_res}
tools:
    - Calculator
    - CodeInterpreter
    - ReadFileContent
    - WriteFileContent
    - ShellTool
    - name: WebSearch
      bing_api_key: ${env|bing_api_key, null}
      llm: ${sub|text_res}
