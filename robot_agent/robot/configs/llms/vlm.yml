name: OpenaiGPTLLM
model_id: ${env| model_id2, gpt-4o-mini}
api_key: ${env| custom_openai_key2, openai_api_key}
endpoint: ${env| custom_openai_endpoint2, https://api.openai.com/v1}
temperature: 0
vision: true
response_format: json_object
